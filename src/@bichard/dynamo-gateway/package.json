{
  "name": "@bichard/dynamo-gateway",
  "description": "Provides a higher level set of abstractions to work with DynamoDB",
  "version": "0.0.1",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@typescript-eslint/eslint-plugin": "^5.11.0",
    "@typescript-eslint/parser": "^5.11.0",
    "eslint": "^8.9.0",
    "jest": "^27.5.1",
    "jest-junit": "^13.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@bichard/types": "file:../types",
    "aws-sdk": "^2.1073.0"
  },
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "jest",
    "test:integration": "jest"
  }
}
