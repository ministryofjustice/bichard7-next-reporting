{
  "name": "@bichard/postgres-gateway",
  "version": "1.0.0",
  "description": "Provides a higher level set of abstractions to work with Postgres",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.40.1",
    "@typescript-eslint/parser": "^5.40.1",
    "eslint": "^8.25.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@bichard/types": "file:../types",
    "pg": "^8.8.0",
    "pg-promise": "^10.12.1"
  }
}
